/** @type {import('next').NextConfig} */
const nextConfig = {
  // Headers are configured in vercel.json to avoid Next/Vercel inconsistencies
  // This ensures consistent header application across all environments
};

module.exports = nextConfig;

